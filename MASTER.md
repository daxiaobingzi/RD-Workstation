# 设计系统 · MASTER.md

> **弱电智能化设计工作台（ELV Engineering Workbench）**
> 全局设计规则的唯一权威来源。任何页面/组件实现都须遵循本文件。
> 目的：统一配色、字体、间距、组件状态与多屏布局，并满足可访问性。

---

## 1. 产品定位 & 设计基调

- **产品类型**：B2B 工程类专业设计工具（弱电智能化 / ELV：项目→子系统→设备点→选型→清单报价）。
- **目标用户**：弱电设计师、造价/采购、项目经理。长时间、高密度、数据密集型操作。
- **关键词**：专业、精确、可信、清爽、轻动效。Glassmorphism（鲥璃质感）承载工程数据的秩序感。
- **设计方向**：工程图签 + 液态玻璃。**蓝**为信任主色、**工程橙**为关键动作 / CTA，状态色（绿/红/黄/青）承载语义。

---

## 2. 配色

### 2.1 语义令牌（按角色命名，明暗双主题各自映射）

| 令牌 | 亮色 | 暗色 | 用途 |
|------|--------------------|--------------------|------|
| `--primary` | `#2563EB` | `#60A5FA` | 主操作、选中、链接、聚焦 |
| `--on-primary` | `#FFFFFF` | `#0B1B3B` | 主强调底色上的文字/图标 |
| `--accent`（工程橙） | `#EA580C` | `#FB923C` | CTA / 强调 / 需突出的关键数据 |
| `--on-accent` | `#FFFFFF` | `#1A1206` | 橙底上的文字/图标 |
| `--background` | `#F6F8FC` | `#0F1522` | 页面底色 |
| `--foreground` | `#1E293B` | `#E6EDF7` | 正文字 |
| `--text2`（次级） | `#475569` | `#A9B7CC` | 次级文字（≥4.5:1） |
| `--text3`（占位） | `#94A3B8` | `#6B7C96` | 辅助/占位（仅辅助信息） |
| `--line` | `#E2E8F0` | `#23324A` | 边框 / 分隔线 |
| `--card` | `rgba(255,255,255,.68)` | `rgba(24,34,54,.68)` | 卡片面（半透玻璃） |
| `--destructive` | `#DC2626` | `#F87171` | 删除、错误、危险 |
| `--ring` | `#2563EB66` | `#60A5FA66` | 键盘聚焦光环 |

### 2.2 状态色（业务语义）

| 令牌 | 亮色 | 暗色 | 语义 |
|------|------|------|------|
| `--green` | `#16A34A` | `#4ADE80` | 成功 / 已完成 / 在线 |
| `--red` | `#DC2626` | `#F87171` | 失败 / 逾期 / 缺项 |
| `--amber` | `#D97706` | `#FBBF24` | 警告 / 待确认 |
| `--cyan` | `#0891B2` | `#22D3EE` | 前端设备 / 标识路由 |

### 2.3 反模式

- ❌ 禁止主题混用硬编码 hex（一律走语义令牌）。
- ❌ 禁止仅用暗色默认（亮色为默认，暗色可选）。
- ❌ 状态色不可只靠颜色区分——需叠加图标/文字/线框。

---

## 3. 字体 & 排版

### 3.1 字体族

- **中文正文/标题**：`"PingFang SC","HarmonyOS Sans SC","Microsoft YaHei"`（系统中文族，避免 Web 字体体积）。
- **拉丁正文/标题**：`"SF Pro text","Plus Jakarta Sans",system-ui`。
- **数字（金额/数量/版本号）**：等宽 `--mono`（tabular-nums）。

```css
--font-body: -apple-system, "SF Pro Text", "Plus Jakarta Sans", "PingFang SC", "HarmonyOS Sans SC", "Microsoft YaHei", sans-serif;
--font-mono: ui-monospace, "SF Mono", "JetBrains Mono", Menlo, Consolas, monospace;
```

### 3.2 字号阶梯（rem）

| 令牌 | 值 | 用途 |
|------|-----|------|
| `--fs-xs` | 12px | 角标 / 辅助说明 |
| `--fs-sm` | 13px | 表格行 / 次要标签 |
| `--fs-md` | 14.5px | 正文 / 控件默认 |
| `--fs-lg` | 17px | 卡片标题 |
| `--fs-xl` | 22px | 区块标题 |
| `--fs-2xl` | 30px | 页面大标题 |
| `--lh` | 1.55 | 正文行高 |

### 3.3 反模式

- ❌ 字号随意堆叠（须走阶梯）。
- ❌ 中文不压缩字距（`letter-spacing:-.02em` 只用于英文大标题）。

---

## 4. 间距（4/8 节奏）

| 令牌 | 值 | 用途 |
|------|-----|------|
| `--space-1` | 4px | 图标间距 |
| `--space-2` | 8px | 图标与文字间隙 / 控件内边距 |
| `--space-3` | 12px | 栅格内小间隔 |
| `--space-4` | 16px | 卡片内边距（默认） |
| `--space-6` | 24px | 区块间距 |
| `--space-8` | 32px | 大区块 / 弹窗内边距 |
| `--radius-sm` | 8px | 输入框 / 芯片 |
| `--radius-md` | 14px | 按钮 / 表格 |
| `--radius-lg` | 20px | 卡片 / 面板 |
| `--radius-full` | 999px | 胶囊 |

---

## 5. 组件状态（所有可交互元素）

| 状态 | 规则 |
|------|------|
| hover | 颜色/亮度过渡，150–280ms，**不引起布局位移**（禁 scale 位移周围内容） |
| pressed | 透明度/下压反馈（≤150ms） |
| focus-visible | 始终可见的聚焦环（`--ring` 2px + offset 2px） |
| disabled | `disabled` 原生语义 + 降重要度（`opacity:.45`）+ `cursor:not-allowed` + 不响应点击 |
| 触摸目标 | 所有可点目标 ≥44px，图标按钮扩大热区 |

> 玻璃表面（半透卡片/弹窗）需用足够强的 scrim 隔离前景，确保文字可读。

---

## 6. 响应式布局

| 断点 | 宽度 | 布局行为 |
|------|------|----------|
| 手机 | ≤767px | 侧栏收起、底栏导航；卡片单列；间距收紧到 12px gutter |
| 平板 | 768–1023px | 卡片规整；侧栏可选折叠 |
| 桌面 | ≥1024px | 侧栏展开；多列表格/网格 |
| 宽屏 | ≥1440px | 内容最大宽度约束，避免超长行 |

- gutter：手机 12–16px，平板上 16–24px，桌面 24–32px。
- 表格：窄屏横向滚动 + 关键列优先；不可挤压无换行。

---

## 7. 可访问性（交付前自查）

- [ ] 正文/次级文字亮度对比 ≥ 4.5:1（明暗各自验证，不推断）。
- [ ] 可点击元素均有 `cursor:pointer`。
- [ ] 键盘 `:focus-visible` 环始终可见、不被遮挡。
- [ ] 装饰性图标 `aria-hidden`；独立图标/图标按钮有可访问名称与状态（选中/按下/展开）。
- [ ] 表单控件有 label、hint、清晰错误信息；失败表单保留内联字段错误并聚焦错误汇总。
- [ ] 颜色不是唯一信息传达方式。
- [ ] `prefers-reduced-motion` 下停用非必要动效并渲染终态。
- [ ] 动效时长统一、退出快于进入。
- [ ] Overlay/固定栏不遮挡焦点与滚动内容。

---

## 8. 交付前清单（复用）

- [ ] 无 emoji 当图标（用统一 SVG 图标集）。
- [ ] 图标同族同风格（线/面一致，层级统一）。
- [ ] 无硬编码颜色（全部语义令牌）。
- [ ] hover 平滑过渡 150–300ms 且不位移布局。
- [ ] 按 4/8px 节奏布局。
- [ ] 375 / 768 / 1024 / 1440 四档验证无横向溢出。
- [ ] 弹窗 scrim 实测隔离度。